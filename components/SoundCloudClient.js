import React, { Fragment } from 'react';

const SoundCloudClient = () => (
  <Fragment>
    <h2>Seed Project?</h2>

    <p>
      If you don’t have the outcome of <a href="http://www.robinwieruch.de/the-soundcloud-client-in-react-redux/">The SoundCloud Client in React + Redux</a>, checkout this <a target="_blank" rel="noopener noreferrer" href="https://github.com/rwieruch/react-redux-soundcloud">repository</a>. Clone the repository and use the Base folder named base-init/ as seed project. The seed project is the same which you would implement when you would step through the <a href="http://www.robinwieruch.de/the-soundcloud-client-in-react-redux/">initial tutorial</a>. Next to the Base folder you will find several Extension folders, which will show the outcome of each tutorial which builds up on The SoundCloud Client in React + Redux.
    </p>

    <p>
      When you have your own <a href="http://www.robinwieruch.de/the-soundcloud-client-in-react-redux/">The SoundCloud Client in React + Redux</a> project, you can continue with the <a href="#toc">Table of Contents</a> and ignore the following instructions.
    </p>

    <p>
      When you want to use the seed project, make sure to clone the <a target="_blank" rel="noopener noreferrer" href="https://github.com/rwieruch/react-redux-soundcloud">repository</a>. Use the base-init/ folder as your project folder. Make sure that your project starts with <code>npm install && npm start</code>.
    </p>
  </Fragment>
);

export default SoundCloudClient;